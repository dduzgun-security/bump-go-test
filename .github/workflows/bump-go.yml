name: Bump Go Version and stdlibs

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  bump:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@a5ac7e51b41094c92402da3b24376905380afc29 #v4.1.6

      - name: Run bump-go
        uses: dduzgun-security/bump-go@a89513b7f9777235f6d7acf81cfc61bcab3d58ad
        with:
          go_version: ".go-version"
          pr_reviewer: "dduzgun-security"
          github_token: ${{ secrets.GITHUB_TOKEN }}
