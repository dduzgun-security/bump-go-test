name: Bump Go Version and stdlibs

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  bump:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@a5ac7e51b41094c92402da3b24376905380afc29 #v4.1.6

      - name: Run bump-go
        uses: dduzgun-security/bump-go@51085d4b408ed00e349c91a2f9c0ef8e01234d83
        with:
          go_version: ".go-version"
          pr_reviewer: "dduzgun-security"
          github_token: ${{ secrets.GITHUB_TOKEN }}
